The data that will be used to compute filters in their original format. 
The folder wpd_projects must contain the WebPlotdigitizer project in .tar format if applicable, and the csv folder must contain the resulting csv files.
